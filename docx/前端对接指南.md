# 前端对接指南

本文档面向前端开发者，说明如何与 acgAgent 后端 API 进行对接。

---

## 1. 架构概览

```
浏览器 → Vite Dev Server (proxy /api) → Gateway (:8080) → acg-user (:8081) / acg-chat (:8082)
```

- 所有请求统一经过 **Gateway（端口 8080）**，前端只需对接这一个入口
- Vite 开发环境已配置代理：`/api` → `http://localhost:8080`，前端请求直接用 `/api` 前缀即可
- Gateway 根据路径自动路由到下游服务，前端无需感知后端微服务拆分

---

## 2. 统一响应格式

所有接口（除 SSE 流式接口外）均返回以下 JSON 结构：

```typescript
interface Result<T> {
  code: number    // 业务状态码
  message: string // 描述信息
  data: T         // 业务数据，错误时为 null
}
```

### 状态码

| code | 含义 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证或 Token 过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 注意事项

- 即使业务错误，**HTTP 状态码仍为 200**，错误通过 JSON 中的 `code` 字段区分
- Gateway 层的鉴权拦截（Token 缺失/无效）返回的是 **HTTP 401**，无 JSON 响应体
- 前端 Axios 拦截器应同时处理 HTTP 401 和业务 code 401

---

## 3. 认证机制

### Token 获取

通过登录接口获取 `accessToken` 和 `refreshToken`：

```json
// POST /api/auth/login 成功响应
{
  "code": 200,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiJ9...",
    "expiresIn": 7200
  }
}
```

### Token 使用

- 需认证的接口须携带请求头：`Authorization: Bearer <accessToken>`
- Gateway 验证 Token 后，将 `userId` 注入 `X-User-Id` 请求头转发给下游服务

### Token 有效期

| Token 类型 | 有效期 |
|---|---|
| accessToken | 2 小时 |
| refreshToken | 7 天 |

### Token 刷新

accessToken 过期后，用 refreshToken 调用刷新接口获取新的 Token 对：

```
POST /api/auth/refresh
Body: { "refreshToken": "<refreshToken>" }
```

### 免认证接口

以下接口无需携带 Token：

| 路径 | 说明 |
|---|---|
| `POST /api/auth/register` | 注册 |
| `POST /api/auth/login` | 登录 |
| `POST /api/auth/refresh` | 刷新 Token |
| `POST /api/auth/sms/send` | 发送短信验证码 |
| `POST /api/auth/sms/login` | 短信验证码登录 |
| `POST /api/auth/wx/qrcode` | 获取微信二维码 |
| `POST /api/auth/wx/callback` | 微信扫码回调 |
| `GET /api/agents` | Agent 列表 |
| `GET /api/agents/{id}` | Agent 详情 |

---

## 4. 枚举值定义

前端开发时需对齐以下枚举：

### CommonStatus（通用状态）

| 值 | 含义 |
|---|---|
| `0` | 禁用 |
| `1` | 启用 |

用于：UserVO.status、RoleVO.status、PermissionVO.status、AgentVO.status

### MessageRole（消息角色）

| 值 | 含义 |
|---|---|
| `"user"` | 用户消息 |
| `"assistant"` | AI 回复 |

### PermissionType（权限类型）

| 值 | 含义 |
|---|---|
| `1` | 菜单 |
| `2` | 按钮 |

---

## 5. API 接口详情

### 5.1 认证模块（`/api/auth`）

#### POST `/api/auth/register` — 用户注册（免认证）

```json
// Request
{ "username": "testuser", "password": "123456" }

// Response: Result<UserVO>
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "testuser",
    "nickname": null,
    "email": null,
    "phone": null,
    "avatar": null,
    "status": 1,
    "createdAt": "2026-01-01T00:00:00",
    "updatedAt": "2026-01-01T00:00:00"
  }
}
```

#### POST `/api/auth/login` — 密码登录（免认证）

```json
// Request
{ "username": "testuser", "password": "123456" }

// Response: Result<TokenVO>
{
  "code": 200,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGci...",
    "refreshToken": "eyJhbGci...",
    "expiresIn": 7200
  }
}
```

#### POST `/api/auth/refresh` — 刷新 Token（免认证）

```json
// Request
{ "refreshToken": "eyJhbGci..." }

// Response: Result<TokenVO>（同登录响应）
```

#### POST `/api/auth/sms/send` — 发送短信验证码（免认证）

```json
// Request
{ "phone": "13800138000" }

// Response: Result<Void>
{ "code": 200, "message": "success" }
```

#### POST `/api/auth/sms/login` — 短信验证码登录（免认证）

```json
// Request
{ "phone": "13800138000", "code": "123456" }

// Response: Result<TokenVO>（同登录响应，手机号未注册时自动注册）
```

#### POST `/api/auth/wx/qrcode` — 获取微信登录二维码（免认证）

```json
// Response: Result<{ url: string, state: string }>
{
  "code": 200,
  "message": "success",
  "data": {
    "url": "https://open.weixin.qq.com/connect/qrconnect?...",
    "state": "abc123"
  }
}
```

#### POST `/api/auth/wx/callback` — 微信扫码登录（免认证）

```json
// Request
{ "code": "wechat_auth_code" }

// Response: Result<TokenVO>（同登录响应，未绑定用户时自动注册）
```

---

### 5.2 用户模块（`/api/users`，需认证）

#### GET `/api/users/me` — 获取当前用户信息

```
// Response: Result<UserVO>
```

#### GET `/api/users` — 获取所有用户列表

```
// Response: Result<UserVO[]>
```

#### GET `/api/users/{id}` — 根据 ID 获取用户

```
// Response: Result<UserVO>
```

#### POST `/api/users` — 创建用户

```json
// Request
{
  "username": "newuser",
  "nickname": "昵称",
  "email": "user@example.com",
  "phone": "13800138000",
  "avatar": "https://example.com/avatar.png",
  "status": 1
}

// Response: Result<UserVO>
```

#### PUT `/api/users/{id}` — 更新用户

```json
// Request（支持部分字段更新）
{ "nickname": "新昵称", "email": "new@example.com" }

// Response: Result<UserVO>
```

#### DELETE `/api/users/{id}` — 删除用户（逻辑删除）

```
// Response: Result<Void>
```

#### POST `/api/users/{userId}/roles` — 分配用户角色

```json
// Request: role ID 数组（全量替换）
[1, 2, 3]

// Response: Result<Void>
```

#### GET `/api/users/{userId}/roles` — 获取用户角色 ID 列表

```json
// Response: Result<number[]>
{
  "code": 200,
  "message": "success",
  "data": [1, 2]
}
```

---

### 5.3 角色模块（`/api/roles`，需认证）

#### GET `/api/roles` — 获取所有角色

```
// Response: Result<RoleVO[]>
```

#### GET `/api/roles/{id}` — 根据 ID 获取角色

```
// Response: Result<RoleVO>
```

#### POST `/api/roles` — 创建角色

```json
// Request
{
  "name": "管理员",
  "code": "admin",
  "sort": 0,
  "status": 1,
  "remark": "超级管理员"
}

// Response: Result<RoleVO>
```

RoleVO 结构：

```typescript
interface RoleVO {
  id: number
  name: string
  code: string
  sort: number
  status: number    // 0=禁用, 1=启用
  remark: string | null
  createdAt: string
  updatedAt: string
}
```

#### PUT `/api/roles/{id}` — 更新角色

```json
// Request（支持部分字段更新）
{ "name": "新角色名" }

// Response: Result<RoleVO>
```

#### DELETE `/api/roles/{id}` — 删除角色

```
// Response: Result<Void>
```

#### POST `/api/roles/{roleId}/permissions` — 分配角色权限

```json
// Request: permission ID 数组（全量替换）
[1, 2, 5, 8]

// Response: Result<Void>
```

#### GET `/api/roles/{roleId}/permissions` — 获取角色权限列表

```
// Response: Result<PermissionVO[]>
```

---

### 5.4 权限模块（`/api/permissions`，需认证）

#### GET `/api/permissions` — 获取所有权限

```
// Response: Result<PermissionVO[]>
```

PermissionVO 结构：

```typescript
interface PermissionVO {
  id: number
  parentId: number  // 父权限 ID，0 表示顶级
  name: string
  code: string      // 权限编码，如 "user:list"
  type: number      // 1=菜单, 2=按钮
  path: string | null    // 前端路由路径
  icon: string | null
  sort: number
  status: number    // 0=禁用, 1=启用
  createdAt: string
  updatedAt: string
}
```

权限数据为**树形结构**，通过 `parentId` 构建层级关系。前端可根据 `type` 区分菜单和按钮权限，用于菜单渲染和按钮级权限控制。

#### GET `/api/permissions/{id}` — 根据 ID 获取权限

```
// Response: Result<PermissionVO>
```

#### POST `/api/permissions` — 创建权限

```json
// Request
{
  "parentId": 0,
  "name": "用户管理",
  "code": "user:list",
  "type": 1,
  "path": "/system/users",
  "icon": "user",
  "sort": 0,
  "status": 1
}

// Response: Result<PermissionVO>
```

#### PUT `/api/permissions/{id}` — 更新权限

```json
// Request（支持部分字段更新）
{ "name": "新权限名" }

// Response: Result<PermissionVO>
```

#### DELETE `/api/permissions/{id}` — 删除权限

```
// Response: Result<Void>
```

---

### 5.5 Agent 模块（`/api/agents`）

#### GET `/api/agents` — 获取 Agent 列表（免认证）

```
// Response: Result<AgentVO[]>
```

#### GET `/api/agents/{id}` — 获取 Agent 详情（免认证）

```
// Response: Result<AgentVO>
```

AgentVO 结构：

```typescript
interface AgentVO {
  id: number
  name: string
  description: string | null
  avatar: string | null
  apiUrl: string       // 外部 LLM API 地址
  apiKey: string       // 响应中始终为 "******"（脱敏）
  model: string        // 模型名称，如 "gpt-4"
  status: number       // 0=禁用, 1=启用
  configJson: string | null  // JSON 字符串，如 '{"temperature": 0.7}'
  createdAt: string
  updatedAt: string
}
```

> **注意**：`apiKey` 在响应中始终被脱敏为 `"******"`，前端不应将其用于显示或回传。
> 更新 Agent 时，传 `"******"` 表示保持原值不变，传新值表示替换。

#### POST `/api/agents` — 创建 Agent（需认证）

```json
// Request
{
  "name": "GPT-4 助手",
  "description": "通用对话助手",
  "avatar": "https://example.com/agent.png",
  "apiUrl": "https://api.openai.com/v1/chat/completions",
  "apiKey": "sk-xxxxx",
  "model": "gpt-4",
  "status": 1,
  "configJson": "{\"temperature\": 0.7}"
}

// Response: Result<AgentVO>
```

#### PUT `/api/agents/{id}` — 更新 Agent（需认证）

```json
// Request
{
  "name": "新名称",
  "apiKey": "******"
  // 传 "******" 保持原 API Key 不变
}

// Response: Result<AgentVO>
```

#### DELETE `/api/agents/{id}` — 删除 Agent（需认证）

```
// Response: Result<Void>
```

---

### 5.6 对话模块（`/api/chat`，需认证）

#### POST `/api/chat/conversations` — 创建对话

```json
// Request
{
  "agentId": 1,
  "title": "帮助我写代码"  // 可选，默认 "New Conversation"
}

// Response: Result<ConversationVO>
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "agentId": 1,
    "title": "帮助我写代码",
    "createdAt": "2026-01-01T00:00:00",
    "updatedAt": "2026-01-01T00:00:00"
  }
}
```

#### GET `/api/chat/conversations` — 获取当前用户对话列表

```
// Response: Result<ConversationVO[]>
// 按创建时间倒序排列，仅返回当前用户的对话
```

#### GET `/api/chat/conversations/{id}/messages` — 获取对话消息列表

```
// Response: Result<MessageVO[]>
```

MessageVO 结构：

```typescript
interface MessageVO {
  id: number
  conversationId: number
  role: 'user' | 'assistant'
  content: string
  tokens: number | null
  createdAt: string
}
```

#### POST `/api/chat/conversations/{id}/send` — 发送消息（SSE 流式）

**此接口与其他接口不同，返回的是 SSE 事件流，不是 `Result<T>` JSON 包装。**

```json
// Request
{ "content": "你好，请帮我写一段代码" }
```

前端应使用 `fetch` + `ReadableStream` 或 `EventSource`（需自行处理 POST）接收流式响应：

```typescript
// 推荐使用 fetch 方式
const response = await fetch(`/api/chat/conversations/${conversationId}/send`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${accessToken}`
  },
  body: JSON.stringify({ content: '你好' })
})

const reader = response.body!.getReader()
const decoder = new TextDecoder()

while (true) {
  const { done, value } = await reader.read()
  if (done) break

  const chunk = decoder.decode(value, { stream: true })
  // 解析 SSE 数据并实时渲染 AI 回复
  // chunk 格式为 SSE 文本流，包含 AI 生成的增量内容
}
```

> **重要**：此接口不走 Axios 拦截器，需手动处理认证和错误。

#### DELETE `/api/chat/conversations/{id}` — 删除对话

```
// Response: Result<Void>
// 仅允许删除自己的对话，否则返回 403
```

---

## 6. 接口总览

| 模块 | 方法 | 路径 | 认证 | 请求体 | 响应类型 |
|---|---|---|---|---|---|
| 认证 | POST | `/api/auth/register` | 否 | `RegisterRequest` | `Result<UserVO>` |
| 认证 | POST | `/api/auth/login` | 否 | `LoginRequest` | `Result<TokenVO>` |
| 认证 | POST | `/api/auth/refresh` | 否 | `RefreshTokenRequest` | `Result<TokenVO>` |
| 认证 | POST | `/api/auth/sms/send` | 否 | `SmsSendRequest` | `Result<Void>` |
| 认证 | POST | `/api/auth/sms/login` | 否 | `SmsLoginRequest` | `Result<TokenVO>` |
| 认证 | POST | `/api/auth/wx/qrcode` | 否 | — | `Result<{url,state}>` |
| 认证 | POST | `/api/auth/wx/callback` | 否 | `WxLoginRequest` | `Result<TokenVO>` |
| 用户 | GET | `/api/users/me` | 是 | — | `Result<UserVO>` |
| 用户 | GET | `/api/users` | 是 | — | `Result<UserVO[]>` |
| 用户 | GET | `/api/users/{id}` | 是 | — | `Result<UserVO>` |
| 用户 | POST | `/api/users` | 是 | `UserVO` | `Result<UserVO>` |
| 用户 | PUT | `/api/users/{id}` | 是 | `UserVO` | `Result<UserVO>` |
| 用户 | DELETE | `/api/users/{id}` | 是 | — | `Result<Void>` |
| 用户 | POST | `/api/users/{userId}/roles` | 是 | `number[]` | `Result<Void>` |
| 用户 | GET | `/api/users/{userId}/roles` | 是 | — | `Result<number[]>` |
| 角色 | GET | `/api/roles` | 是 | — | `Result<RoleVO[]>` |
| 角色 | GET | `/api/roles/{id}` | 是 | — | `Result<RoleVO>` |
| 角色 | POST | `/api/roles` | 是 | `RoleVO` | `Result<RoleVO>` |
| 角色 | PUT | `/api/roles/{id}` | 是 | `RoleVO` | `Result<RoleVO>` |
| 角色 | DELETE | `/api/roles/{id}` | 是 | — | `Result<Void>` |
| 角色 | POST | `/api/roles/{roleId}/permissions` | 是 | `number[]` | `Result<Void>` |
| 角色 | GET | `/api/roles/{roleId}/permissions` | 是 | — | `Result<PermissionVO[]>` |
| 权限 | GET | `/api/permissions` | 是 | — | `Result<PermissionVO[]>` |
| 权限 | GET | `/api/permissions/{id}` | 是 | — | `Result<PermissionVO>` |
| 权限 | POST | `/api/permissions` | 是 | `PermissionVO` | `Result<PermissionVO>` |
| 权限 | PUT | `/api/permissions/{id}` | 是 | `PermissionVO` | `Result<PermissionVO>` |
| 权限 | DELETE | `/api/permissions/{id}` | 是 | — | `Result<Void>` |
| Agent | GET | `/api/agents` | **否** | — | `Result<AgentVO[]>` |
| Agent | GET | `/api/agents/{id}` | **否** | — | `Result<AgentVO>` |
| Agent | POST | `/api/agents` | 是 | `AgentVO` | `Result<AgentVO>` |
| Agent | PUT | `/api/agents/{id}` | 是 | `AgentVO` | `Result<AgentVO>` |
| Agent | DELETE | `/api/agents/{id}` | 是 | — | `Result<Void>` |
| 对话 | POST | `/api/chat/conversations` | 是 | `CreateConversationRequest` | `Result<ConversationVO>` |
| 对话 | GET | `/api/chat/conversations` | 是 | — | `Result<ConversationVO[]>` |
| 对话 | GET | `/api/chat/conversations/{id}/messages` | 是 | — | `Result<MessageVO[]>` |
| 对话 | POST | `/api/chat/conversations/{id}/send` | 是 | `SendMessageRequest` | **SSE 流** |
| 对话 | DELETE | `/api/chat/conversations/{id}` | 是 | — | `Result<Void>` |

---

## 7. TypeScript 类型参考

```typescript
// ===== 通用 =====
interface Result<T> {
  code: number
  message: string
  data: T
}

// ===== 认证 =====
interface LoginRequest {
  username: string
  password: string
}

interface RegisterRequest {
  username: string
  password: string
}

interface RefreshTokenRequest {
  refreshToken: string
}

interface TokenVO {
  accessToken: string
  refreshToken: string
  expiresIn: number
}

interface SmsSendRequest {
  phone: string
}

interface SmsLoginRequest {
  phone: string
  code: string
}

interface WxLoginRequest {
  code: string
}

// ===== 用户 =====
interface UserVO {
  id: number
  username: string
  nickname: string | null
  email: string | null
  phone: string | null
  avatar: string | null
  status: number       // 0=禁用, 1=启用
  createdAt: string
  updatedAt: string
}

// ===== 角色 =====
interface RoleVO {
  id: number
  name: string
  code: string
  sort: number
  status: number       // 0=禁用, 1=启用
  remark: string | null
  createdAt: string
  updatedAt: string
}

// ===== 权限 =====
interface PermissionVO {
  id: number
  parentId: number
  name: string
  code: string         // 如 "user:list"
  type: number         // 1=菜单, 2=按钮
  path: string | null  // 前端路由路径
  icon: string | null
  sort: number
  status: number
  createdAt: string
  updatedAt: string
}

// ===== Agent =====
interface AgentVO {
  id: number
  name: string
  description: string | null
  avatar: string | null
  apiUrl: string
  apiKey: string       // 响应中始终为 "******"
  model: string
  status: number
  configJson: string | null
  createdAt: string
  updatedAt: string
}

interface CreateAgentRequest {
  name: string
  description?: string
  avatar?: string
  apiUrl: string
  apiKey: string
  model: string
  status?: number
  configJson?: string
}

// ===== 对话 =====
interface ConversationVO {
  id: number
  userId: number
  agentId: number
  title: string
  createdAt: string
  updatedAt: string
}

interface MessageVO {
  id: number
  conversationId: number
  role: 'user' | 'assistant'
  content: string
  tokens: number | null
  createdAt: string
}

interface CreateConversationRequest {
  agentId: number
  title?: string
}

interface SendMessageRequest {
  content: string
}
```

---

## 8. 前端对接注意事项

### 8.1 Axios 拦截器

前端 `request.ts` 已配置了请求/响应拦截器：
- **请求拦截**：自动从 `localStorage` 读取 `accessToken` 并附加到 `Authorization` 头
- **响应拦截**：解析 `Result<T>` 结构，`code !== 200` 时弹出 `ElMessage.error`
- **401 处理**：清除 Token 并跳转登录页

### 8.2 SSE 流式接口

`POST /api/chat/conversations/{id}/send` 返回 SSE 流，不走 Axios，需使用 `fetch` API 手动处理。注意：
- 需手动附加 `Authorization` 请求头
- 需手动处理流式读取和解析
- 网络错误需自行捕获

### 8.3 Agent apiKey 脱敏

Agent 的 `apiKey` 字段在所有响应中都被脱敏为 `"******"`：
- 前端不应将 `"******"` 显示给用户或作为真实值回传
- 更新 Agent 时传 `"******"` 表示保持原值，传新值表示替换

### 8.4 角色与权限分配

角色和权限的分配接口采用**全量替换**模式：
- `POST /api/users/{userId}/roles` 传入的数组会完全替换原有角色
- `POST /api/roles/{roleId}/permissions` 传入的数组会完全替换原有权限
- 前端应先获取当前列表，让用户增删后，提交完整的新列表

### 8.5 权限树构建

权限数据是扁平结构，通过 `parentId` 构建树形关系。前端构建菜单时可参考：

```typescript
function buildPermissionTree(list: PermissionVO[]): PermissionTreeNode[] {
  const map = new Map<number, PermissionTreeNode>()
  const roots: PermissionTreeNode[] = []

  list.forEach(item => {
    map.set(item.id, { ...item, children: [] })
  })

  map.forEach(node => {
    if (node.parentId === 0) {
      roots.push(node)
    } else {
      map.get(node.parentId)?.children.push(node)
    }
  })

  return roots
}
```

### 8.6 跨域

Gateway 已配置 CORS（允许所有来源），开发环境通过 Vite 代理可避免跨域问题。生产环境建议通过 Nginx 反向代理统一域名。
